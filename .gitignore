# Server binaries and runtime data (not part of the repository)
/server/
/txData/
/cache/
/crash/
/run/

# Logs
*.log

# Secrets — never commit these
server.cfg.local
.env

# Disabled resource: huge map tiles and a black-screen bug, not shipped
/resources/\[brighton\]/bu-bigmap/
