Config = Config or {}

-- Всё, что добавляет Brighton Union, должно быть видно на карте.
-- Здесь — блипы мест, у которых нет собственных в других ресурсах.

Config.Blips = {
    { coords = vector3(208.0, -1640.0, 29.8), sprite = 436, color = 1, scale = 0.7, name = 'Пожарная станция' }
}
