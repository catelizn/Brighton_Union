# bu-map

One rule on Brighton Union: everything we add is visible on the map. This
resource holds blips for locations that don't create their own — currently the
fire station. Add new entries to `Config.Blips` in `config.lua`.
