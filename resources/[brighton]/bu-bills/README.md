# bu-bills

Hourly utility bills for property owners: businesses (`bu_properties`),
apartments (`bu_apartments`) and houses (`player_houses`). Charged from the
bank account, skipped with a notification when the balance is too low.
Amounts and interval are in `server/main.lua`.
