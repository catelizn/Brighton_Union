# bu-gps

Navigation hints for the map waypoint: distance, turns and arrival notice,
drawn above the minimap. Text version of the voice assistant — Russian voice
lines can be plugged into `client/main.lua` later (PlaySound with a custom
audio pack).
